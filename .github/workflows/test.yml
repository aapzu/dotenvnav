name: Test
on:
  push:
    branches: 
      - main
  pull_request:
    branches: 
      - main
  workflow_call:
jobs:
  install:
    uses: ./.github/workflows/install.yml`
  test:
    needs: install
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: pnpm ci
