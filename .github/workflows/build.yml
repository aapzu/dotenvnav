name: Build
on:
  push:
    branches: 
      - main
  pull_request:
    branches: 
      - main
  workflow_call:
jobs:
  install:
    uses: ./.github/workflows/install.yml
  build:
    needs: install
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: pnpm build
